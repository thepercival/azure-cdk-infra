name: Deploy Core Infrastructure in Azure

on:
  push:
    branches-ignore:
      - main
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  deploy-dev:
    if: github.event_name == 'push'
    uses: ./.github/workflows/reusable-deploy.yml
    with:
      environment: dev
      resource-group: rg-core-dev
    secrets: inherit

  deploy-acc:
    if: github.event.pull_request.merged == true
    uses: ./.github/workflows/reusable-deploy.yml
    with:
      environment: acc
      resource-group: rg-core-acc
    secrets: inherit

  deploy-prd:
    needs: deploy-acc
    uses: ./.github/workflows/reusable-deploy.yml
    with:
      environment: prd
      resource-group: rg-core-prd
    secrets: inherit
